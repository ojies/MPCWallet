name: threshold
description: FFI bindings to the threshold-rs cryptography library for FROST threshold signatures on secp256k1.
version: 2.0.0

environment:
  sdk: '>=3.0.0 <4.0.0'

dependencies:
  ffi: ^2.2.0
  convert: ^3.1.1
  crypto: ^3.0.3

dev_dependencies:
  lints: ^4.0.0
  test: ^1.25.6
