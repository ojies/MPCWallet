name: client
description: A library for client-side MPC wallet operations.
version: 1.0.0
publish_to: none

environment:
  sdk: '>=3.3.0 <4.0.0'

dependencies:
  grpc: ^5.1.0
  protobuf: ^6.0.0
  threshold:
    path: ../threshold
  protocol:
    path: ../protocol
  bitcoin_base: ^7.0.0
  hive: ^2.2.3
  convert: ^3.1.2
  blockchain_utils: ^5.4.0
  fixnum: ^1.1.1
  path: ^1.9.1

dev_dependencies:
  test: ^1.24.0
  lints: ^3.0.0
